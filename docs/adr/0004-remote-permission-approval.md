# Out-of-Band Remote Permission Delegation

We decided to capture PC-side AI Agent pending permission requests (file deletion, package installation, shell execution) and delegate approval to the mobile client via out-of-band push notifications and in-app confirmation cards.
