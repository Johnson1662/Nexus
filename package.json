{
  "name": "anywhere",
  "version": "0.1.0",
  "description": "Agent bridge - WebSocket server + terminal chat client",
  "scripts": {
    "build": "cd server && tsc && node -e \"require('fs').cpSync('src/registry/agents.json','dist/registry/agents.json',{dereference:true});console.log('[build] copied agents.json')\"",
    "start": "node server/dist/server.mjs",
    "server:old": "node server/server.js",
    "chat": "node client.js",
    "demo": "echo '1. Run: npm run start   (in terminal 1)\n2. Run: npm run chat    (in terminal 2)\n3. Type a prompt to start opencode\n4. Commands: /quit /cancel /model <name>\n5. Prefix with claude/codex to use other agents'"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.35.0",
    "@agentclientprotocol/sdk": "^0.21.1",
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/ws": "^8.18.1",
    "tree-kill": "^1.2.2",
    "typescript": "^6.0.3"
  }
}
